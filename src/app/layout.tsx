import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GoDash - Gimnasio Digital de Ventas",
  description: "Entrena tus habilidades de ventas con simulaciones IA hiperrealistas. Mejora tu tasa de cierre en 15-25% en 90 días.",
  keywords: ["ventas", "sales training", "AI", "simulación", "roleplay", "capacitación comercial"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
